/// Controls how matrix elements are displayed.
export enum EDisplayElements
{
	/// Matrix element values are never displayed.
	Off,

	/// Values are only displayed once modified.
	ModifiedOnly,

	/// Values are always displayed.
	All
}

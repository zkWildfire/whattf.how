/// Struct that defines the results of a game.
export interface GameResults
{
	/// The number of characters that were typed correctly.
	correctCharacters: number;

	/// The number of points that were earned.
	points: number;
}
